apiVersion: batch/v1
kind: Job
metadata:
  name: setting-up-kcrealm
  namespace: keycloak
spec:
  template:
    spec:
      serviceAccountName: cronjob
      containers:
      - name: kcrealm
        image: katonic/nb-isolation:v1
        command: ["env-setup.sh"]
      restartPolicy: Never
      imagePullSecrets:
       - name: docker-hub-secret
  backoffLimit: 4
